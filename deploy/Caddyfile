# 反向代理 + 自动 HTTPS(Let's Encrypt)
# 域名从环境变量 CADDY_DOMAIN 注入(见 .env)
# 一个域名同时提供:后台界面(/) 和 后端 API(/api/v1)
# 因为 web(nginx)容器内部已把 /api 反代到后端,这里只需指向 web 即可
{$CADDY_DOMAIN} {
	encode gzip
	reverse_proxy web:80
}
